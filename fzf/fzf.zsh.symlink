# Setup fzf
# ---------
if [[ ! "$PATH" == */home/pnavais/.fzf/bin* ]]; then
  export PATH="$PATH:/home/pnavais/.fzf/bin"
fi

# Auto-completion
# ---------------
[[ $- == *i* ]] && source "/home/pnavais/.fzf/shell/completion.zsh" 2> /dev/null

# Key bindings
# ------------
source "/home/pnavais/.fzf/shell/key-bindings.zsh"

